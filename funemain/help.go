package funemain

var (
	usageline = `usage: fune [flags]
       start an fune agent

       fune --version
       show the version of fune

       fune -h | --help
       show the help information about fune
	`

	flagsline = `
logging flags:
	--debug 'false'
		enable debug-level logging for etcd.
	`
)
